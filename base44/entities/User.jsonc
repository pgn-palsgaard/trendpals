{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "submitter",
        "reviewer"
      ],
      "default": "user",
      "description": "App role. 'admin' = full access. 'user' = read-only access to libraries. 'submitter' = Submit Brief page only. 'reviewer' = external SME, gated to the review portal only."
    }
  }
}